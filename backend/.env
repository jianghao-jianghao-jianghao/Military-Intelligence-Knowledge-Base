
# --- Project: Military Knowledge Base ---
# Environment: Local Development

# --- Application ---
PROJECT_NAME="Military Brain"
API_V1_STR="/api/v1"
DOMAIN="localhost"
ENVIRONMENT="dev"

# --- Security (CHANGE THESE IN PRODUCTION) ---
# Generate with: openssl rand -hex 32
SECRET_KEY="dev_secret_key_change_this_in_production_environment"
# Token expiration in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=1440 
ALGORITHM="HS256"

# --- Database (PostgreSQL + Age + Vector) ---
POSTGRES_SERVER=db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=military_secret
POSTGRES_DB=military_kb
POSTGRES_PORT=5432
# Connection String used by SQLAlchemy
DATABASE_URL=postgresql+asyncpg://postgres:military_secret@db:5432/military_kb

# --- Caching (Redis) ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=""

# --- Object Storage (MinIO) ---
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
# Bucket Names
MINIO_BUCKET_INTERNAL="military-internal"
MINIO_BUCKET_SECRET="military-secret"

# --- AI & LLM Services ---
# OpenAI or Compatible (vLLM/LocalAI)
OPENAI_API_KEY="sk-..."
OPENAI_API_BASE="https://api.openai.com/v1"
EMBEDDING_MODEL="text-embedding-3-small"
EMBEDDING_DIMENSION=1536

# --- Admin Initial Setup ---
FIRST_SUPERUSER=admin
FIRST_SUPERUSER_PASSWORD=admin
